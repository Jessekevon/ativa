alert('Project created!');
// Set up an anonymous function that will run automatically.
(function() {
	var projectHtml = "<%= j render('show', :project => @project) %>";
	$('.projects').append(projectHtml);
})();